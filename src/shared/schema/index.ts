import * as auth from "@/shared/schema/auth.schema";
import * as common from "@/shared/schema/common.schema";
import * as user from "@/shared/schema/user.schema";

export const schema = {
  auth,
  common,
  user,
};
